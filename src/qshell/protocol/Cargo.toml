[package]
name = "microblossom-qshell-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Record codec for the host-driven MicroBlossom QShell baseline"

[lib]
path = "src/lib.rs"
